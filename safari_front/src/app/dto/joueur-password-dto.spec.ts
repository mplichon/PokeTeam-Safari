import { JoueurPasswordDto } from './joueur-password-dto';

describe('JoueurPasswordDto', () => {
  it('should create an instance', () => {
    expect(new JoueurPasswordDto()).toBeTruthy();
  });
});
